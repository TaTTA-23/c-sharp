//A instrução switch escolhe uma seção de código a ser executada em uma lista de seções de comutador possíveis. A seção de comutador selecionada é escolhida com base em uma correspondência de padrão com a expressão de correspondência da instrução.
/*
O código declara uma variável fruit do tipo string e a inicializa com o valor "apple".
O bloco switch verifica o valor de fruit e executa o bloco de código correspondente ao caso que coincide com o valor de fruit.
Se fruit for "apple", ele imprime "App will display information for apple." e sai do switch devido ao break.
Se fruit for "banana", ele imprime "App will display information for banana." e sai do switch.
Se fruit for "cherry", ele imprime "App will display information for cherry." e sai do switch.
Se fruit não corresponder a nenhum dos casos anteriores, o bloco default é executado, imprimindo "App will display information for unknown fruit."
*/
using System;

public class Program
{
    public static void Main(string[] args)
    {
    string fruit = "apple"; 
    
     switch (fruit)
     
        {
        case "apple":
            Console.WriteLine($" App will display information for apple.");
            break;
        case "banana":
            Console.WriteLine($" App wil display information for banana.");
        break;
        
        case "cherry":
            Console.WriteLine($" App will display information for cherry.");
            break;
        default:
            Console.WriteLine($"App will display information for unknown fruit.");
                break;

        }  
    }
}
